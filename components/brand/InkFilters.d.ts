/**
 * Hidden <defs> carrying the brand's three SVG filters: `wc-edge` (painterly edges),
 * `wc-bleed` (large soft blobs) and `sketchy` (hand-drawn diagram strokes).
 * Mount once near the root of any page that uses Button, TagPill or the sketchy variant.
 */
export function InkFilters(): JSX.Element;
