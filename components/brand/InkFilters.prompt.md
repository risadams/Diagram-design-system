Mount once per page, before anything that references `filter: url(#wc-edge)`.

```jsx
<><InkFilters /><Button>Read the guide</Button></>
```

`sketchy` goes on a `<g>` wrapping diagram *shapes* — never on text, never on dark parchment.
