Index and gallery entries — a diagram thumbnail, a post, a profile.

```jsx
<Card meta="2026-09-10 · architecture" title="Content site in production"
      excerpt="Reader → edge → origin, with the cache doing the work." splash="teal">
  <svg viewBox="0 0 480 260">…</svg>
</Card>
```

No border, no shadow, no rounded panel. If it needs a box, it isn't this component.
