Groups nodes into a labeled region. Required once a figure passes nine nodes.

```jsx
<DiagramZone x={616} y={128} width={164} height={272} label="content" />
```

Render zones first (behind arrows and nodes), 2–4 per figure, hairline only — no fills heavier than 2% ink.
