Use for dev-tool and CLI-product cards, not editorial figures.

```jsx
<TerminalWindow name="loop.sh — self-improving-loop" command="diagram-design render --type loop" title="The self-improving loop">
  <svg viewBox="0 0 1120 520">…</svg>
</TerminalWindow>
```

Inside it, swap tokens 1:1 for their `terminal-*` equivalents and run every text role 1–2px above the standard ramp. One rose titlebar dot — never a traffic-light triad.
